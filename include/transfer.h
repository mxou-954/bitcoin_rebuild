#ifndef TRANSFER_H
#define TRANSFER_H

#include <string>
#include <vector>
#include "entities.h"

using namespace std;

void transfer(std::vector<std::string> user_command_part, User currentUser, std::vector<User> users);

#endif
